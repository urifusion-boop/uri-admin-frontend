/**
 * Backend service URL prefixes
 * These are prepended to all API routes
 */
export enum BackendUrlEnum {
  INSIGHTS = '/uri-insights',
  BACKEND = '/api/v1',
  TRANSACTIONS = '/uri-transactions/api/v1',
  TASK_MANAGER = '/task-manager/api/v1',
}
